function longestSubstring(s) {
    let seenChars = new set() ; 
    let left = 0;
    let right = 0;
    let maxlength = 0;
    while(right< s.length){
        if(!seenChars.has)
    }
}

module.exports = { longestSubstring };


